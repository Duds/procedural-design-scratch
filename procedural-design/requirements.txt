# Core dependencies (required)
numpy>=1.21.0
scipy>=1.7.0

# 3D mesh processing (required)
trimesh>=3.9.0
scikit-image>=0.19.0

# Visualization (required)
matplotlib>=3.4.0

# CLI (required)
click>=8.0.0

# Optional dependencies
# GPU acceleration - enables 10-100x speedup for Gray-Scott simulations
# CPU fallback available if not installed
taichi>=1.7.0

# Jupyter environment (required for notebooks)
jupyterlab>=4.0.0
notebook>=6.4.0
ipywidgets>=8.0.0
jupyterlab-widgets>=3.0.0

# Web app (required for Streamlit interface)
streamlit>=1.28.0

# Optional: Advanced mesh operations (igl)
# Only needed for certain advanced mesh operations
# Install separately if required: conda install -c conda-forge libigl pyigl
# pyigl  # Not available via pip, use conda

# Development dependencies (not required for runtime)
# Testing
pytest>=7.0.0
pytest-cov>=3.0.0

# Code quality
black>=22.0.0
flake8>=4.0.0
mypy>=0.950